# TASKS.md — Second Vision AI Active Tasks

> **Last updated:** 2026-08-21
>
> This document tracks the current working tasks. For the full phased roadmap, see [PLAN.md](file:///Users/luna/Projects/Thesis/second-vision-ai/docs/PLAN.md). For a function-by-function breakdown of how the Stage 5.3-5.9 scripts actually work, see [SCRIPTS_STAGE_5.3-5.9.md](file:///Users/luna/Projects/Thesis/second-vision-ai/docs/SCRIPTS_STAGE_5.3-5.9.md).

---

## Current Phase: Phase 2, Stage 5.3-5.9 pipeline plumbing complete — curation in progress before the final cascade

Phases 0-1 complete. **Stage 5.1 (Acquisition) and Stage 5.2 (Conversion) are both 100% complete** as of 2026-08-13 — all 5 sources (Open Images, Roboflow, ExDark, Dataset Ninja ×2, CrowdHuman) pulled and converted to DEC-046's intermediate schema, real data on disk, verified for real at every step.

Unattended overnight session (`.agents/handoff-2026-08-13-stage-5.3-through-5.9.md`) built and ran **every Stage 5.3-5.9 script** marked "Build" in that handoff. Since then (2026-08-18, DEC-069/070/071/072): added a 12th Trash Bins source (eyecue, 559 images) and a second Doors source (`door_detection_zqt59`, 4,493 images — likely resolves the Doors bottleneck), fixed a real `cap_per_class.py` starvation bug (a priority source could fully exhaust a shrunken hard_cap and zero out the next one), formally closed traffico_y1, moved the split ratio to 70/15/15, and re-ran `cap_per_class.py`→`merge.py` with the eyecue/starvation-fix combination (52,756 images merged, up from 51,529 — **does not yet include `door_detection_zqt59`**). `notebooks/fiftyone_review_processed.ipynb` now also browses `dataset/merged/`, `dataset/final/<split>/`, flagged-only views from `box_audit.py` reports, and — new — a write-back cell that saves in-App review edits (box fixes/deletions/additions) to a `labels_reviewed/` staging folder per source, verified against simulated edits.

**Current state**: the full-scale `dedup.py` run was killed (DEC-072, student's call) after surviving a laptop-sleep stall but not recovering its throughput (~9h ETA at the time, and another pass would've been needed regardless once `door_detection_zqt59` folds in). `cap_per_class.py`→`merge.py`→`dedup.py`→`split.py`→`generate_yaml.py` all need one final rerun, deliberately deferred until curation settles — running them mid-review would either miss `door_detection_zqt59`/elevator corrections, or (for `merge.py` specifically, mid-dedup) risk corrupting a still-running dedup read. See `docs/OPEN_QUESTIONS.md` for the standing list of decisions that still block specific scripts; check it before assuming something is a fresh question.

**2026-08-20 (DEC-082)**: the DEC-076 full-pool review turned up a real problem bigger than box-shape polish — `stairs_i2yia`, `escalator_stairs`, and `elevator_status_s4lrk` all failed on direct visual review (boxes not on the annotated object at all, duplicate/near-duplicate images, or corrupted source images) and are now `audit_status: failed`. Two replacement sources found, SDK-checked, pulled, and converted: `stair_gaptw` (1,564 images, clean) and `elevator_status_0iq4p` (1,461 images/2,868 boxes, with a custom split mapping — its "People are in the elevator." label maps to canonical Person, not Elevator, geometrically verified post-convert). Escalator's schema slot was left reserved, not renumbered, pending a replacement class.

**2026-08-20, later same day (DEC-083)**: Escalator's slot filled with **Shelf** (Open Images, 22,899 boxes/6,011 images) after browsing Shelf/Cart/Countertop candidates in `fiftyone_preview.ipynb`'s new ad-hoc preview section — Cart and Countertop rejected on real visual inspection (horse-drawn carts, kitchen counters). Pole switched from 2 unreviewed Roboflow sources to Open Images `"Street light"` (44,697 boxes/11,226 images). Five new Roboflow sources added, SDK-checked, pulled, converted (Potholes: `pothole_voxrl`; Pedestrian Lane: `wtf_dwvgm` + `revised_pedestrian_obstacle`; Vehicle/Motorcycle/Tricycle: `dlsu_d_vehicle_type_detection` + `roitrikee`) — real bug caught mid-pass (a bare-list `native_class_filter` silently blanket-mapped every native class instead of filtering, caught from the conversion report's own dropped-count before trusting it, fixed). Four superseded Roboflow sources benched (`pole_detection_z76mb`, `utility_poles_44tzx`, `pothole_vhmow`, `pedestrian_and_animal_crossing`) — real post-add numbers confirmed none need reinstating (Potholes 2,661, Pedestrian Lane 2,745, Stairs 2,294 — the last one a real unplanned bonus from `revised_pedestrian_obstacle`'s own `stairs` class). **A real DEC-number collision from a concurrent session was found and fixed** (renumbered to DEC-084) — this repo's split-agent setup means two sessions can edit the same docs at once; don't assume any single session's view is complete. Next real blocker: same cascade rerun as before (`cap_per_class.py`→`merge.py`→`dedup.py`→`split.py`→`generate_yaml.py`), now also needs to pick up all of today's source changes — still deliberately deferred until curation settles.

**2026-08-21 (DEC-085)**: student asked for a direct re-verification of DEC-082/083/084 rather than trusting the summary. Found and fixed one real bug: `wtf_dwvgm` had 4 stale, mislabeled files left on disk from the pre-fix buggy run (the converter never cleans up files that no longer qualify after a mapping change) — deleted, now matches its own report exactly (1,326 images/1,592 boxes). Pedestrian Lane's active total corrected 2,745 → **2,741** as a direct consequence. Separately found `classes.yaml`'s per-class source documentation had gone stale for 7 classes (Stairs, Potholes, Vehicle, Motorcycle, Tricycle, Pedestrian Lane, Elevator, plus Doors from an earlier session) — confirmed this never affected pipeline behavior (Roboflow acquisition/conversion is driven by `datasets.yaml`, not `classes.yaml`'s provider lists) but was actively misleading as documentation; resynced all 7. Flagged one pre-existing, unresolved risk: Potholes' 2,661 total depends on `dataset_ninja_road_damage_detector` counting as active, but that source has no `audit_status` field and its own notes describe it as a dormant DEC-034 fallback — without it, Potholes sits at 1,330, **below the 1,500 floor**. Not resolved, added to `docs/OPEN_QUESTIONS.md` (#11) for the student's call. Everything else re-checked (Pole/Shelf/all 5 new sources' real counts, Stairs/Vehicle/Motorcycle/Tricycle/Person totals, no duplicate DEC headers) matched exactly, no further changes needed.

**2026-08-21, later same day (DEC-086)**: student's answer to DEC-085's flagged question — activate `dataset_ninja_road_damage_detector` ("we need that fallback now"). Set explicit `audit_status: approved`. Tracing whether this would actually change anything surfaced a much bigger bug: **`cap_per_class.py` had no `audit_status` awareness anywhere** — `discover_processed_sources()` → `build_class_index()` → `cap_class()` scanned every `dataset/processed/<source>/` unconditionally, meaning the 7 sources benched/failed today/yesterday (`stairs_i2yia`, `escalator_stairs`, `elevator_status_s4lrk`, `pole_detection_z76mb`, `utility_poles_44tzx`, `pothole_vhmow`, `pedestrian_and_animal_crossing`) would all have silently re-entered the candidate pool on the next real cap/merge run, contradicting what DEC-082/083 both claimed. Fixed with a new `get_inactive_processed_source_keys()` in `config_loader.py`, wired into `cap_per_class.py` only (deliberately not the shared `discover_processed_sources()`, since `box_audit.py` legitimately still wants to see failed sources for diagnostics). Verified via `--dry-run`: Potholes/Stairs/Pedestrian Lane/Pole candidates now land exactly on the numbers DEC-083/085 already documented, with all 7 sources correctly excluded. Confirmed the last *real* run (DEC-077, 8/18) predates this risk — not retroactively contaminated, this only affects the still-pending next run.

**2026-08-21, later still (DEC-087)**: student revised Person/Vehicle/Motorcycle's source priority order and asked to deactivate any review-checklist `[x]` source (`me5_u6rvg`, `augmented_tricycle` named explicitly), clean up `revised_pedestrian_obstacle`'s known motorcycle contamination, and promote `cv_project_hovyc`/`trashcan_detection_pihfn`'s already-reviewed labels ahead of the next merge. `CLASS_PRIORITY_SOURCES` updated for all 3 classes; a `--dry-run` before finalizing caught Open Images being squeezed to 0 selected images for Person/Vehicle once the other priority sources filled the cap — flagged via AskUserQuestion rather than assumed, student chose to give Open Images its own explicit floor too (375-500 guaranteed images per class instead of 0). `me5_u6rvg`/`augmented_tricycle` benched; `elevator_awvus` deliberately kept active despite also being `[x]`-marked — DEC-082 already independently confirmed it clean, and the checklist's stale note (still citing the overturned DEC-031 concern) was corrected instead of blindly deactivating a known-good source. Promoted `cv_project_hovyc`/`trashcan_detection_pihfn`'s `labels_reviewed/` over the real `labels/` (originals backed up first — `dataset/processed/` is gitignored, no git safety net). That promotion surfaced a real, fully-investigated-before-trusted finding: both sources are no longer single-class — the review added correct, visually-verified boxes for other classes visible in the same photos (`cv_project_hovyc` now also contributes Person/Stairs/Chairs/Vehicle/Pole/Trash Bins/Bicycle/Tables/Animals/Pedestrian Lane/Motorcycle; `trashcan_detection_pihfn` also contributes Person/Vehicle/Chairs) — ruled out a concurrent-session file change first (mtime + recent-modification checks), then spot-checked 2 real samples to confirm the new boxes are legitimate, not review mistakes.

---

## Active Tasks

### ✅ Phase 0: Repository Initialization (Complete)

- [x] Create folder structure
- [x] Write `README.md`
- [x] Write `AGENTS.md`
- [x] Write `PROJECT.md`
- [x] Write `PLAN.md`
- [x] Write `DECISIONS.md` (DEC-001–005, DEC-012–024)
- [x] Write `TASKS.md`
- [x] Create `.gitignore`
- [x] Create `config/classes.yaml` (v3 — OI primary, local pull)
- [x] Create `config/datasets.yaml` (per-source config, pinned versions)
- [x] Scaffold directory structure with `.gitkeep` files
- [x] Align all documentation with HANDOFF v3 + D-018r + DEC-024
- [x] Create `requirements.txt` with pinned dependencies — exists on disk (2026-08-04); stale checkbox, actually done before this session started
- [ ] Initial commit and push — commits exist locally, but `main` has no upstream/remote configured yet, so nothing has actually been pushed

---

### ⬜ Phase 1: Configuration & Tooling (Up Next)

> Scope narrowed by DEC-025 (2026-08-06) — see docs/DECISIONS.md.

- [x] Create `requirements.txt` — FiftyOne, Roboflow SDK, Ultralytics, supervision, etc.
- [x] Install and verify FiftyOne
- [x] Install and verify Roboflow SDK
- [ ] Verify CVAT/Label Studio integration via FiftyOne
- [x] Build `scripts/utils/config_loader.py` — YAML config reader for classes.yaml + datasets.yaml
- [x] Build `scripts/utils/file_utils.py` — path helpers, safe copy/move
- [x] Build `scripts/utils/bbox_utils.py` — ExDark + CrowdHuman raw box parsing, validate/clip guard (DEC-025: narrowed — VOC/COCO-style/YOLO sources use FiftyOne's native importers instead of custom conversion code)
- [ ] ~~Build `scripts/utils/image_utils.py`~~ — dropped per DEC-025: corrupt-image checks via `compute_metadata()`, dedup via FiftyOne Brain, no standalone module
- [x] Create `config/training.yaml` — YOLOv8s baseline hyperparameters

---

### ⬜ Phase 2: Dataset Pipeline (Blocked by Phase 1)

#### Stage 5.1: Acquisition
- [x] Verify Open Images native class name strings (Blocker #5 scope) — resolved 2026-08-08, DEC-029
- [x] Build `scripts/acquire/acquire_openimages.py` (FiftyOne Zoo, 8 classes — Motorcycle/Bicycle split per DEC-038) — built + smoke-tested 2026-08-10, DEC-040
- [x] Run full `acquire_openimages.py` for real — all 8 classes pulled 2026-08-13, DEC-051. 0 errors, 9.7GB total. Trash Bins came in low (1,106, ~18% of request) — flags DEC-039's revisit trigger, not acted on yet
- [x] Build `scripts/acquire/acquire_crowdhuman.py` — scripted download+parse (DEC-048, reverses DEC-041's manual-only stance for this source now that it's HF-hosted, public/ungated, real SDK)
- [x] Run full `acquire_crowdhuman.py` for real — 2026-08-13, DEC-056. 0 errors, 21GB raw/11GB processed. 19,370 images, 439,046 boxes, 18,917 clipped — exact match to the earlier annotation-only dry-run prediction. The one previously-unverified assumption (`Images/<ID>.jpg` zip layout) confirmed correct against real bytes
- [x] Build `scripts/acquire/acquire_exdark.py` — parse-only (DEC-041); built + run for real 2026-08-13, DEC-046. 6,042 images / 18,366 boxes converted to `dataset/processed/exdark/`, canonical class ids applied, 41 boxes clipped, non-canonical classes (Boat/Bottle/Bus/Cup) correctly dropped
- [x] Build `scripts/acquire/acquire_datasetninja.py` — parse-only (DEC-041); built + run for real 2026-08-13, DEC-049. Both sources placed and converted: pothole_detection 665/665 images, road_damage_detector 1331/3321 (rest had no pothole-class object). Format turned out genuine Supervisely (corner-pair boxes), not Pascal VOC as previously assumed — parsed directly, no SDK needed
- [x] Build `scripts/acquire/acquire_roboflow.py` (pinned SDK versions) — built + smoke-tested 2026-08-13, DEC-045. Eligibility rule (`pending` + pinned version) pulls 11/16 projects, skips 5 with reasons.
- [x] Run full `acquire_roboflow.py` for real — all 11 eligible projects pulled 2026-08-13, 0 errors, 2.6GB total, all counts verified against `datasets.yaml`'s documented estimates
- [x] Pin versions for all Roboflow projects in `datasets.yaml` — resolved 2026-08-08 via `list_roboflow_versions.py` + DEC-036 (splits discarded regardless, so version choice = latest by default). Only remaining exception: `traffico_y1` (zero generated versions, needs checking on Roboflow's side). `me5_u6rvg` reactivated and pinned (v1, DEC-037); `trash_bins` secondary benched, not pursued (DEC-039)

#### Stage 5.2: Conversion
- [x] Build `scripts/convert/openimages_to_intermediate.py` — built + run for real 2026-08-13, DEC-052. Filters each class-folder to its own native classes (raw exports weren't pre-filtered — 213 categories present in the "animals" export, not just Dog/Cat), merges boxes for images pulled under >1 class-folder (2,758 of them) instead of dropping on collision. 26,715 unique images, 67,760 boxes, 81 clipped, 0 invalid. Final per-class image counts audited (non-exclusive)
- [x] Build `scripts/convert/yolo_to_intermediate.py` — built + run for real 2026-08-13, DEC-053/DEC-055. Real per-project format audit found 4 mismatches vs. `datasets.yaml`'s assumptions (escalator_stairs, cv_project_hovyc, me5_u6rvg placeholder class names recovered via API, pedestrian_and_animal_crossing's needed class missing from its only export). Forked + regenerated the last one on Roboflow to pull in the real class (retried once after a genuine dropped-connection failure). **All 11/11 projects converted for real: 38,426 images, 52,304 boxes total.** Fixed a real report-overwrite bug found along the way (`--projects` subset runs now merge into the existing report instead of clobbering it)
- ~~CrowdHuman/ExDark/Dataset Ninja converters~~ — not needed, folded into their acquire scripts (DEC-041, DEC-046, DEC-049)

#### Stage 5.3: Box Audit (includes Roboflow audit — D-018r)
- [x] Build `scripts/preprocess/box_audit.py` — built + run for real 2026-08-13, DEC-057. Also found + fixed a real `clip_bbox()` bug (see Completed table)
- [x] Run box audit on all `native_unspecified` sources — covered, one script audits every source regardless of bbox_mode (DEC-057)
- [x] Run box audit on all `project_dependent` sources — same script, same run (DEC-057)
- [~] Resolve audit: `elevator-status-s4lrk` (detection vs classification) — heuristic built, flagged list produced (844 boxes), student review still needed (docs/OPEN_QUESTIONS.md #3)
- [ ] Resolve audit: `traffico-y1` (full class list dump) — no data on disk (never pulled, blocked on Roboflow's side), nothing to audit
- [x] Resolve audit: `pedestrian-and-animal-crossing` (class names) — already resolved DEC-053/055, confirmed still current
- [ ] Near-exhaustive review for smaller Roboflow pools — student's visual-review time, not a script gap (docs/OPEN_QUESTIONS.md #4)

#### Stage 5.4: Per-Class Capping
- [x] Build `scripts/preprocess/cap_per_class.py` (3-tier fill logic) — built + run for real 2026-08-13, DEC-058. Decision/report only (see DEC-058 for why) — writes `dataset/reports/cap_report.json`, doesn't move files
- [x] Revise per-class source priority — 2026-08-15, DEC-067 (supersedes DEC-058's plain-random default for Person/Vehicle/Elevator). Generalized ExDark-only floor to per-class `CLASS_PRIORITY_SOURCES` + an instance sub-budget mechanism for crowdhuman's extreme density. Re-run for real: crowdhuman 128→729 images (Person), me5_u6rvg 1,649→3,188 images (Vehicle), elevator_awvus fully included over defect-flagged elevator_status_s4lrk. **`dataset/reports/cap_report.json` regenerated; `dataset/merged/` and everything downstream NOT yet re-run — pending go-ahead**
- [x] Run capping — verify ExDark guaranteed floor applied — verified: ExDark floor reserved first for all 7 real-data-derived overlap classes, dense classes (Person, Chairs) correctly hit instance_target before image cap
- [x] Add configurable `--hard-cap {1500,4500,9000}` preset (default 4500) — 2026-08-17, DEC-068. Floor now derived as `hard_cap // 3` (DEC-042's own ratio invariant) instead of a fixed constant; `INSTANCE_TARGET` deliberately left unscaled (no formula exists). Real run at the 4500 default reproduces DEC-067's numbers exactly (verified no-op); 1500/9000 verified via `--dry-run` only, writing to non-clobbering `cap_report_hardcap<N>.json` files, not run for real yet. Surfaced (not fixed) a real gap: at `--hard-cap 1500`, Person's ExDark priority source alone exhausts the shrunken budget and starves crowdhuman — now detected via a printed `WARNING`, still an open question how to fairly split a shrunken cap across priority sources
- [ ] Review `dataset/reports/cap_report.json` — student review pending; ratio invariant not met (4.08 vs 3:1 target, Trash Bins-driven, already flagged `docs/OPEN_QUESTIONS.md` #1)

#### Stage 5.6: Merge + Dedup
- [x] Build `scripts/build/merge.py` — built + run for real 2026-08-13, DEC-059/061. Corrects DEC-058's assumption: README.md documents `dataset/merged/` as "post-cap", so this merges `cap_report.json`'s selected union, not raw `processed/`. Code-review pass (DEC-061) found + fixed a stale-output bug; re-run clean
- [x] Build `scripts/preprocess/dedup.py` — built + run for real 2026-08-14, DEC-062
- [x] Run merge with source-prefixed filenames — 51,529/51,529 merged (post-DEC-061 fix), 0 missing, verified on disk (file count matches report exactly)
- [x] Run cross-source dedup (highest risk pairs flagged) — exact-duplicates on full 51,529-image pool: 1,893 groups/2,143 files. Near-duplicates on seeded 6,000-image stratified sample (threshold=0.2, FiftyOne Brain default): 520 groups/818 files. Report at `dataset/reports/dedup_report.json`; student review pending (docs/OPEN_QUESTIONS.md #7)

#### Stage 5.5: Model-Assisted Curation
- [x] Build `scripts/curate/run_mistakenness.py` (FiftyOne) — built + run for real 2026-08-13, DEC-060. COCO-pretrained yolov8n proxy model, 7/16 classes with a COCO analog
- [ ] Build `scripts/curate/reimport_corrections.py` (CVAT/LS) — **Skipped**, genuinely blocked on tool choice (`docs/OPEN_QUESTIONS.md` #5)
- [x] Run mistakenness on capped pools — 22,846 images scored, ranked report at `dataset/reports/mistakenness_report.json`
- [ ] Review and correct flagged samples in CVAT/Label Studio — student review pending
- [ ] Re-import corrections into `dataset/curated/` — blocked on the above

#### Stages 5.7–5.9: Final Curation → Split → YAML
- [x] Build & run `final_merge_curation.py` — built + run for real 2026-08-14, DEC-064. Reuses `run_mistakenness.py`'s crosswalk directly (DEC-060). 22,824 images scored on the corrected merged pool; top-ranked pattern matches Stage 5.5's finding (open_images Chairs/Tables under-annotation signal persists post-merge). Report at `dataset/reports/final_merge_curation_report.json`; student review pending (docs/OPEN_QUESTIONS.md #5)
- [x] Build & run `split.py` — built + run for real 2026-08-14, DEC-063. Found + fixed a real union-find bug in duplicate-group leakage prevention (56 groups initially straddled splits; fixed, re-run confirmed 0 leakage). train=38,691/val=6,383/test=6,455, verified on disk (51,529 total matches split_report.json)
- [x] Build & run `generate_yaml.py` — built + run for real 2026-08-14, DEC-065. `dataset/final/data.yaml` written, verified matching `config/classes.yaml`'s authoritative `names:` field exactly (not its separately-ordered `classes:` metadata block). Mechanical — no new decisions. **Stage 5.3-5.9 "Build" scope now 100% complete except `reimport_corrections.py` (blocked, docs/OPEN_QUESTIONS.md #5)**

---

## Backlog

See [PLAN.md](file:///Users/luna/Projects/Thesis/second-vision-ai/docs/PLAN.md) Phases 3–7 for training through deployment.

---

## Blocked Items

| Item | Blocked By | Notes |
|------|-----------|-------|
| Acquisition scripts | Phase 1 utilities | Converters depend on shared config loader + utilities |
| Roboflow acquisition | Version pinning | Must identify and pin version numbers first |
| Audit-flagged projects | Stage 5.3 box audit | `traffico-y1`, `elevator-status-s4lrk` resolved at box audit |
| Cap/merge pipeline | Stage 5.3 box audit | Audit must pass before capping |
| Training notebook | Phase 2 Stage 5.9 | Need final merged dataset before training |
| Hailo compilation | Phase 4 ONNX export | Need exported ONNX model first |

---

## Open Blockers (from HANDOFF v3 Section 7, updated)

| # | Blocker | Status |
|---|---------|--------|
| 1 | Trash Bins secondary Roboflow project — TBD | 🔵 Benched 2026-08-08 (DEC-039) — Open Images primary judged sufficient; revisit only if evaluation shows it's not |
| 2 | traffico-y1 full class audit | ✅ Resolved 2026-08-08 — DEC-032 |
| 3 | elevator-status-s4lrk audit | 🟡 Partially resolved 2026-08-08 (DEC-031) — usable subset confirmed, full isolation still needs Stage 5.3 |
| 4 | pedestrian-and-animal-crossing class names | ⬜ Resolve during Stage 5.3 |
| 5 | Open Images native class name verification | ✅ Resolved 2026-08-08 — DEC-029 |
| 6 | Stairs — 4 sources quality comparison | 🟡 Partial finding 2026-08-08 (DEC-031) — 2 of 4 sources show box-shape issue, marked `failed`; full comparison still after box audit |
| 7 | Pothole source: Dataset Ninja vs. new Roboflow candidates | ✅ Resolved 2026-08-08 — DEC-034: combine both, Dataset Ninja currently blocked (Dropbox rate limit), retry later |

---

## Completed

| Task | Date | Notes |
|------|------|-------|
| Repository structure created | 2026-07-31 | Folders per spec, `.gitignore` configured |
| Documentation initialized | 2026-07-31 | README, AGENTS, PROJECT, PLAN, DECISIONS, TASKS |
| `config/classes.yaml` v1 created | 2026-07-31 | 15-class canonical schema (original) |
| HANDOFF v3 alignment | 2026-08-04 | Full doc overhaul — DEC-012–023, classes.yaml v2, datasets.yaml |
| D-018r + DEC-024 alignment | 2026-08-06 | Fork→local pull, Objects365→Open Images, pipeline to 9 stages |
| Repo hygiene pass + AGENTS.md mentor-mode update | 2026-08-06 | Removed stale `curate.py`, fixed README clone URL, added `models/current/`, DEC-002 status note, Documentation & Recording Policy + student-mentor collaboration rules added to AGENTS.md |
| `file_utils.py` + `bbox_utils.py` built (DEC-025 scope) | 2026-08-07 | Path helpers/safe copy-move; ExDark+CrowdHuman box conversion + validate/clip guard. Both smoke-tested clean |
| FiftyOne + Roboflow SDK verified, `config/training.yaml` created | 2026-08-07 | fiftyone 1.20.0 (pre-installed) + roboflow 1.4.0 (installed) both import cleanly together (roboflow pinned numpy/opencv-python-headless down slightly — no conflict). Baseline training config parses clean |
| Source re-audit round: Stairs/Elevator box-shape findings, traffico-y1 resolved, Jeepney consolidated, Potholes sourcing resolved, Pole/Potholes dual-source added | 2026-08-08 | DEC-031 through DEC-035. `docs/preprocessing.md` created for findings log + model-assisted labeling procedure. Dataset Ninja `pothole-detection` confirmed blocked (Dropbox rate limit) via live attempt, not just guessed |
| Jeepney secondary reworked, `Two Wheeler` split into `Motorcycle` + `Bicycle` | 2026-08-08 | DEC-037 (jeep_hozhs benched, me5_u6rvg reactivated), DEC-038 (schema: nc 15→16, ID 2 renamed Two Wheeler→Motorcycle, new ID 15 Bicycle). Propagated across classes.yaml, datasets.yaml, config_loader.py, AGENTS.md, README.md, PROJECT.md, DECISIONS.md |
| Trash Bins secondary search benched | 2026-08-08 | DEC-039 — Blocker #1 closed as benched, not resolved. Open Images `"Waste container"` primary judged sufficient for now |
| `scripts/acquire/acquire_openimages.py` built | 2026-08-10 | DEC-040 — pulls all 8 open_images-primary classes via FiftyOne Zoo, bounded by `max_samples` (DEC-027) computed from each class's `classes.yaml` cap × 1.35 buffer factor (DEC-030). Exports per-class COCO-style `images/ + labels.json` to `dataset/raw/open_images/<class>/`. Smoke-tested end-to-end (real 3-image pull + COCO export verified, then cleaned up) |
| Per-class image cap set to 4,500 (floor 1,500, 3:1 ratio) | 2026-08-11 | DEC-042 — literature-grounded replacement for the old ad-hoc 3500/5000 numbers. `config/classes.yaml`'s `cap` field updated for all 16 classes (uniformly 4500, no more `null`). `acquire_openimages.py` needed no code change — `max_samples` recalculates automatically via `compute_max_samples()` |
| `acquire_openimages.py` filters `IsDepiction`/`IsGroupOf` detections | 2026-08-11 | DEC-043 — student-written edit (guided), verified for real: filtered a 30-image Chair pull against an unfiltered baseline, confirmed exactly 31 detections removed (16 depiction + 15 group-of, no overlap) and 1 fully-emptied image correctly dropped |
| Two exploration notebooks built: `fiftyone_explore.ipynb`, `fiftyone_preview.ipynb` | 2026-08-11 | Post-export browser (loads an already-acquired class folder back into FiftyOne, no network) vs. pre-acquisition tuner (pulls a class at real buffered volume directly from the Zoo to preview filters before running the real script). Both tested end-to-end for real |
| `acquire_openimages.py` corrected to pull all 3 Open Images splits | 2026-08-13 | DEC-044 — was only pulling `train`, contradicting DEC-036's own consequence that acquire scripts should pull every split. Fixed: buffered total now split evenly across train/validation/test. Verified for real (5+5+5=15) |
| `scripts/acquire/acquire_roboflow.py` built | 2026-08-13 | DEC-045 — pulls 11/16 eligible Roboflow projects (`pending` + pinned version), skips the other 5 with reasons. Real pull verified (`cv_project_hovyc`, 1,341 images, correct yolov8 folder structure) |
| Stage 5.2 intermediate schema defined; `scripts/acquire/acquire_exdark.py` built | 2026-08-13 | DEC-046 — flat images/+labels/ pool, canonical class ids, no split. Reused `bbox_utils.py` (built DEC-025, unused until now) for clamping — a gap the student's found reference script (wraphex/ExDark2Yolo) has. Full real run: 6,042 images, 18,366 boxes, 41 clipped, output verified on disk |
| CrowdHuman mirror switched to HuggingFace, Kaggle pseudo-label alternative rejected | 2026-08-13 | DEC-047 — original Baidu/Google Drive mirrors dead. HF mirror confirmed genuine (real vbox/fbox/hbox, matches official stats) via API before switching; a Kaggle version was rejected for using YOLOv8-model-generated pseudo-labels, which would undermine DEC-028's reason for using CrowdHuman at all |
| `scripts/acquire/acquire_crowdhuman.py` built (scripted, via `huggingface_hub`) | 2026-08-13 | DEC-048 — reverses DEC-041's manual-only stance for this one source (HF mirror is public/ungated, real SDK). Verified for real: both annotation files downloaded + parsed (19,370 images, 439,046 boxes kept), filter logic confirmed against real tag/extra.ignore data, box-conversion math verified against a real annotation entry. Image zips (~14GB) left for a deliberate full run |
| `scripts/acquire/acquire_datasetninja.py` built — Stage 5.1 acquisition scripts now all built | 2026-08-13 | DEC-049 — real format turned out Supervisely (corner-pair boxes), not Pascal VOC as `datasets.yaml` assumed; parsed directly, `supervisely`/`dataset_tools` SDK rejected as unnecessary. `bbox_utils.xyxy_to_yolo` promoted from private. Full real run both sources: 665/665 + 1331/3321 images, all output verified on disk, all labels correctly class id 11 (Potholes) |
| `acquire_openimages.py` reruns now clean stale exports; `fiftyone_preview.ipynb` regression fixed | 2026-08-13 | DEC-050 — verified `max_samples` is a hard slice at the FiftyOne source level; found + fixed a real bug where reruns left orphaned image files (labels.json correctly updated, image files didn't); fixed the preview notebook's broken `ZOO_SPLIT` import (stale since DEC-044's rename). All three verified for real |
| Full `acquire_roboflow.py` run — all 11 eligible projects | 2026-08-13 | DEC-051 — run in parallel with the full `acquire_openimages.py` run (independent processes, no shared state, student explicitly OK'd running concurrently). 0 errors, 2.6GB, all 11 counts cross-checked against `datasets.yaml`'s documented per-project estimates |
| CrowdHuman/ExDark/Dataset Ninja scoped to manual download | 2026-08-10 | DEC-041 — checked each source's actual distribution mechanism (CrowdHuman: Baidu/Google Drive mirrors, no API; ExDark: single static archive; Dataset Ninja: SDK is a thin wrapper around a Dropbox host that already failed live). All three rescoped to parse-only scripts; student downloads manually into `dataset/raw/<source>/` first. `datasets.yaml` updated with exact files needed and target paths |
| `scripts/convert/openimages_to_intermediate.py` built — Stage 5.2 begun | 2026-08-13 | DEC-052 — found the per-class raw exports weren't actually pre-filtered to their own class (213 categories present in the "animals" folder alone); found 2,758 images independently pulled under >1 class-folder (chairs∩tables = 1,578) and merged their boxes rather than dropping on collision, per student's explicit call. Real run: 26,715 unique images, 67,760 boxes, 81 clipped. Added a non-exclusive final per-class image + instance (box) count audit per student's request. `docs/PLAN.md`'s stale Stage 5.2 table (still listing 3 already-folded-in converters as Todo) corrected in the same pass |
| `scripts/convert/yolo_to_intermediate.py` built — Stage 5.2 nearly done | 2026-08-13 | DEC-053 — real per-project format audit found 4 mismatches vs. `datasets.yaml` (escalator_stairs filter keys, cv_project_hovyc's undeclared Exit-signage classes, me5_u6rvg's placeholder class names recovered via Roboflow API, pedestrian_and_animal_crossing's needed class missing from its only export). Forked + regenerated that last project on Roboflow to pull in the real class. 10/11 projects converted for real (36,268 images, 49,694 boxes); `pedestrian_and_animal_crossing` pending a stalled download (student's network, not a bug) |
| Notebooks moved to `notebooks/`, labeled by pipeline stage; `fiftyone_review_processed.ipynb` built | 2026-08-13 | DEC-054 — `fiftyone_explore.ipynb` only supported raw COCO-style exports, not the intermediate schema every processed source now uses; new notebook builds the FiftyOne dataset manually from `images/`+`labels/` (canonical ids) rather than fighting FiftyOne's split-oriented YOLOv5 importer. Verified for real against `dataset_ninja_pothole_detection` (50 images, 142 detections, correct `Potholes` label). All 3 existing notebooks fixed for path-robustness after the move and given a clear top-of-notebook purpose label; `notebooks/README.md` added |
| `pedestrian_and_animal_crossing` fork retried successfully; Roboflow Stage 5.2 100% done | 2026-08-13 | DEC-055 — first attempt failed on a genuine dropped connection (`BrokenPipeError`), retried cleanly once the student's network stabilized. Real class name changed on export (Roboflow sanitizes special characters: `==============================` → `------------------------------`) — caught by checking the real `data.yaml`, not assumed. Converts to 2,158 images / 2,610 boxes, exact match to the API's instance count. Also fixed a real bug: `--projects` subset reruns were overwriting the full report instead of merging into it |
| CrowdHuman pulled for real — **Stage 5.1 and Stage 5.2 both 100% complete** | 2026-08-13 | DEC-056 — 0 errors, 21GB raw/11GB processed, 19,370 images / 439,046 boxes / 18,917 clipped. Exact match to the earlier annotation-only dry-run prediction. The one previously-flagged-unverified assumption (`Images/<ID>.jpg` zip layout) confirmed correct against real bytes. All 5 acquisition sources and both Stage 5.2 converters are now fully done — next up is Stage 5.3 (Box Audit) |
| Notebooks moved to `notebooks/`, labeled by pipeline stage; `fiftyone_review_processed.ipynb` built | 2026-08-13 | DEC-054 — `fiftyone_explore.ipynb` only supported raw COCO-style exports, not the intermediate schema every processed source now uses; new notebook builds the FiftyOne dataset manually from `images/`+`labels/` (canonical ids) rather than fighting FiftyOne's split-oriented YOLOv5 importer. Verified for real against `dataset_ninja_pothole_detection` (50 images, 142 detections, correct `Potholes` label). All 3 existing notebooks fixed for path-robustness after the move and given a clear top-of-notebook purpose label; `notebooks/README.md` added |
| `scripts/preprocess/box_audit.py` built and run — Stage 5.3 begun (unattended overnight session) | 2026-08-13 | DEC-057 — source-agnostic, auto-discovers all 16 `dataset/processed/<source>/` dirs, flags box-shape (Tukey-fence elongation/area outliers) and reports global class balance. Real run: 581,879 boxes audited, 5 genuine invalid (all crowdhuman), 0 elsewhere. Found + fixed a real bug along the way: `bbox_utils.clip_bbox()` only clamped one corner per axis, so a box entirely outside the frame degenerated to negative width/height instead of zero — fixed to clamp both corners independently, plus a post-clip re-check added to all 5 converters that call it. The 5 already-existing bad boxes in `dataset/processed/crowdhuman/` were left in place (not regenerated) per the "don't touch processed/ destructively" rule — now correctly flagged for downstream stages. elevator_status_s4lrk's flagging heuristic (`docs/OPEN_QUESTIONS.md` #3) built and run: real data showed no near-full-frame area cluster (max area fraction 0.595, no bimodal gap) — the actual defect is shape/elongation (842 of 844 flagged boxes), matching DEC-031's "polygon-derived, not axis-aligned" finding. Full flagged list written to `dataset/reports/elevator_status_s4lrk_flagged.json` for student review |
| `scripts/preprocess/cap_per_class.py` built and run — Stage 5.4 (unattended overnight session) | 2026-08-13 | DEC-058 — decision/report only (writes `dataset/reports/cap_report.json`, doesn't move files — see DEC-058 for why, ties to Stage 5.5/5.7's same report-only posture). ExDark floor reserved first (derived from real data, not a hardcoded class list), then DEC-042's cap/instance-target/seeded-random-trim fills the rest. Real run confirms DEC-042's own prediction: Person and Chairs hit the 10,000-instance target before the 4,500-image cap (dense classes); most others hit the image cap or include every candidate. Ratio invariant not met (4.08 vs 3:1, Trash Bins-driven) and DEC-042's cap-recompute trigger fired but was deliberately not applied — both already the student's call per `docs/OPEN_QUESTIONS.md` #1 |
| `scripts/build/merge.py` built and run — Stage 5.6 begun (unattended overnight session) | 2026-08-13 | DEC-059 — corrects DEC-058's assumption after finding README.md documents `dataset/merged/` as "post-cap": merges `cap_report.json`'s selected union (51,556 images), not raw `processed/`. Source-prefixed, full original multi-class labels preserved (not stripped to just the selecting class). 0 missing files, verified on disk against real merged label content. Real post-merge per-class counts recomputed (supersede cap_report.json's pre-merge estimates) — e.g. Person 3,653 img (up from its own 2,820 cap selection, "bonus" boxes from other classes' selections). Found + fixed a real O(N×M) glob-per-file performance bug shared with run_mistakenness.py |
| `scripts/curate/run_mistakenness.py` built and run — Stage 5.5 (unattended overnight session) | 2026-08-13 | DEC-060 — COCO-pretrained yolov8n proxy model (no trained model of this project's own exists yet), restricted to the 7/16 canonical classes with an unambiguous COCO analog (crosswalk derived from `classes.yaml`'s own native_class fields, not invented). 22,846 images scored, ranked by mistakenness, full list in `dataset/reports/mistakenness_report.json`. Real finding: highest-mistakenness samples are open_images Chairs/Tables images with more model detections than ground-truth boxes (plausible under-annotation). Second finding: 17.5% of samples score exactly -1.0 (FiftyOne's no-matched-pair sentinel, not a [0,1] score) clustering on crowded CrowdHuman scenes — matches DEC-028's documented COCO-model blind spot on crowds |
| Code review pass on all new Stage 5.3-5.9 scripts — 5 real bugs found and fixed | 2026-08-13 | DEC-061 — ran `/code-review` (6 parallel angles) per the handoff's own suggestion. Found and fixed: a DEC-050-shaped stale-output bug in `merge.py` (didn't clear its output dir before re-writing, leaving 8,590 orphaned files from a prior run — caught by checking real `find`/`wc -l` counts against the script's own tally, not trusting it); `cap_per_class.py`'s ExDark-floor selection bypassing the seeded RNG; an unguarded `canonical_names[class_id]` IndexError risk in `merge.py`/`split.py`; `merge.py`'s `per_source_counts` counting attempted instead of merged files; missing `try/finally` around 3 scripts' scratch FiftyOne dataset cleanup. Also consolidated `build_image_index`/`discover_sources`, each duplicated across 2 files, into new `file_utils.py` helpers. `cap_per_class.py` and `merge.py` re-run for real against the fixed code — `dataset/merged/` now 51,529 images (down slightly from 51,556, the RNG fix's expected effect), verified clean on disk |
| `scripts/preprocess/dedup.py` built and run — Stage 5.6 dedup (unattended overnight session) | 2026-08-14 | DEC-062 — `compute_exact_duplicates()` (filehash, no threshold) against the full 51,529-image merged pool: 1,893 groups/2,143 duplicate files. `compute_near_duplicates(threshold=0.2)` — FiftyOne Brain's own documented default, not invented — against a seeded (42), per-source-proportional stratified sample of 6,000 images (`mobilenet-v2-imagenet-torch` embeddings on MPS, `num_workers=4` pinned): 520 groups/818 flagged files. Sampling (not full-pool) for the near-duplicate check was a measured necessity, not a shortcut — an unbounded run degraded from ~89 img/s to under 10 img/s within 90s once FiftyOne's default `num_workers` over-subscribed this machine's single MPS device; pinning `num_workers=4` helped but didn't fully restore benchmark throughput at full scale. Real finding investigated via FiftyOne Brain's own source: some reported near-duplicate `distance` values exceed the 0.2 threshold because `neighbors_map` reports distance to the nearest *surviving unique* neighbor (a post-hoc query), not the original flagging neighbor — documented as a caveat, not a bug. Report at `dataset/reports/dedup_report.json`; student review pending (`docs/OPEN_QUESTIONS.md` #7) |
| `scripts/build/split.py` built and run — Stage 5.8 (unattended overnight session) | 2026-08-14 | DEC-063 — 75/12.5/12.5 source-stratified split, duplicate groups from `dedup_report.json` (DEC-062) kept together via union-find. First real dry-run's own leakage check caught a real bug: 56 duplicate groups straddled splits because overlapping exact-/near-duplicate groups (223 shared filenames, 187 with genuinely different membership — the two dedup.py checks have different coverage) weren't merged transitively, just last-write-wins overwritten. Fixed with a proper union-find; re-run confirmed 0 leakage. Real result: train=38,691/val=6,383/test=6,455 (74.7%/12.4%/12.5%), all 16 classes present proportionally in all 3 splits, verified against real files on disk (extension-aware count: 1,645 `.jpeg` + 48,758 `.jpg` + 1,126 `.png` = 51,529, exact match to `split_report.json`) |
| `scripts/curate/final_merge_curation.py` built and run — Stage 5.7 (unattended overnight session) | 2026-08-14 | DEC-064 — reuses `run_mistakenness.py`'s COCO crosswalk/logic directly (DEC-060) against the merged pool instead of duplicating it. 22,824 images scored (yolov8n proxy inference + `compute_mistakenness`), report at `dataset/reports/final_merge_curation_report.json` (5.4MB). Real finding: top-ranked pattern matches Stage 5.5 almost exactly — highest scores are open_images Chairs/Tables images with more model detections than ground-truth boxes (top score 0.9701, gt=15 pred=17), confirming the under-annotation signal persists post-merge. 17.5% score exactly -1.0 (FiftyOne's no-match sentinel), same proportion as Stage 5.5 but `roboflow_me5_u6rvg` now contributes 400 of them. Verified 0 leftover FiftyOne datasets after run |
| `scripts/build/generate_yaml.py` built and run — **Stage 5.9, Stage 5.3-5.9 Build scope complete** (unattended overnight session) | 2026-08-14 | DEC-065 — mechanical, no new decisions. `dataset/final/data.yaml` written against the real, populated split (train=38,691/val=6,383/test=6,455). Verified for real against `config/classes.yaml`'s authoritative `names:` field (not its separately-ordered `classes:` metadata block, a real point of possible confusion caught by checking both) — exact match, all 16 classes, correct id order. `config/training.yaml`'s pre-existing `data:` reference now resolves to real data. Not a signal the dataset is training-ready as-is — see `docs/OPEN_QUESTIONS.md` for what's still pending student review |
| Second code review pass (post-DEC-061) on dedup.py/split.py/final_merge_curation.py/generate_yaml.py — 14 findings, highest-severity ones fixed | 2026-08-14 | DEC-066 — most consequential finding, verified against the real installed `ultralytics` package: `generate_yaml.py`'s `path: "."` did NOT resolve relative to the yaml's own directory as claimed, only relative to the training process's CWD — would have broken training if invoked from anywhere but `dataset/final/` itself. Fixed by omitting `path`; verified end-to-end via `check_det_dataset()` from an unrelated CWD (`/tmp`), all 3 splits resolved correctly. Also fixed: `split.py` had reintroduced the DEC-050/061 stale-output bug (no `rmtree` before writing `dataset/final/`) and its own leakage check was print-only, not a hard gate — both fixed. `dedup.py`/`final_merge_curation.py` were missing the project's own `IMAGE_EXTENSIONS` filter when listing images (verified this hadn't corrupted already-completed runs — no stray files existed at run time). Tracking parity added across `split.py`/`final_merge_curation.py` for missing labels/images and unknown class ids, matching `merge.py`'s existing pattern. A `--limit 50` smoke test taken to verify the dedup.py fix accidentally overwrote the real `dedup_report.json` with smoke-test numbers (caught immediately by checking `images_checked`, not assumed harmless) — fixed by re-running dedup.py at full scale again, numbers reproduced identically (deterministic/seeded). 3 lower-severity findings (a DRY duplication between `run_mistakenness.py`/`final_merge_curation.py`, a latent small-n split-math edge case not triggered by real data, a pre-existing unguarded-int-parse pattern shared with `merge.py`) deliberately left as documented follow-ups, not fixed |
| Batch resolution of `docs/OPEN_QUESTIONS.md` (#1/#2/#6/#8) — new Trash Bins source, traffico_y1 closed, real starvation bug fixed, split ratio changed | 2026-08-18 | DEC-069 — checked 2 candidate Roboflow trash-bin projects via SDK before adding either: `eyecue/trashcan-detection-pihfn` (clean single-class, added+pulled+converted, Trash Bins 1,104→1,663 images) vs. `ronits-workspace-e52mh/nyb` (real concerns — mixed pest/wildlife-monitoring classes, non-monotonic versions — deliberately NOT added, left for the student). **New finding**: Doors (1,337), not Trash Bins, is now the ratio-invariant minimum. traffico_y1 formally benched (redundant with me5_u6rvg, DEC-053). `cap_per_class.py`'s priority-source starvation root-caused (ExDark's unbounded *image* floor, not crowdhuman's *instance* subbudget, was the real cause) and fixed generally via a per-source minimum reservation — verified as an exact no-op at the default 4500 cap, verified it actually fixes the 1500-cap starvation case. Split ratio changed to 70/15/15 (Ultralytics Academy's documented baseline) after confirming via WebSearch that Ultralytics does NOT auto-split during training (`autosplit()` is optional/manual only) — `split.py` is necessary, not redundant. Standing rule added to `AGENTS.md`: verify Ultralytics training-loop claims against real docs, don't assume. `cap_per_class.py`+`merge.py` re-run with everything combined: 52,756 images merged (up from 51,529) |
| `fiftyone_review_processed.ipynb` extended for merged/final + flagged-only review; full-scale dedup attempted | 2026-08-18 | DEC-070 — notebook now also browses `dataset/merged/` and `dataset/final/<split>/`, plus a flagged-only mode that loads a `box_audit.py`-style report and marks the *specific* flagged detections (not just flagged images) — verified for real: `elevator_status_s4lrk_flagged.json`'s 844 flagged entries across 750 images load exactly right (caught and fixed a bug in my own verification query along the way, not the notebook itself). Added `dedup.py --full-scale` per the student's explicit request to run the near-duplicate check over the whole pool locally — **launched, but projecting ~5 hours**, well past the "over an hour" expected; left running (killing loses the already-complete exact-duplicate results too, no incremental checkpointing) but flagged prominently rather than left silent. `split.py`/`generate_yaml.py` reruns against the fresh merged pool are blocked on this resolving |
| Second Doors source added (`door_detection_zqt59`, student-sourced); dedup background process re-diagnosed after laptop sleep | 2026-08-18 | DEC-071 — SDK-checked before adding (same discipline as eyecue): real classes are `door`(1908)/`knob`(376)/`hinged`(1739)/`lever`(1239), only `door` unambiguous, pulled+converted with that filter — 4,493 images/4,888 boxes kept, 8,643 non-canonical dropped. Sits in `dataset/processed/`, **not yet merged** (`merge.py` must not run while the dedup background process is still reading `dataset/merged/` — it `rmtree`s that directory first). Confirmed the dedup process (PID 48994) survived the student's laptop sleep — still running, progress 6%→36% — but post-resume throughput hasn't recovered (~1.1 samples/s sustained vs. the original 3.1/s), so the live ETA is now **~9h remaining**, worse than DEC-070's estimate, not a display artifact. Student's dedup decision (let finish / kill for RunPod / kill and accept stale sample) re-asked with this context |
| Full-scale dedup killed (student decision); write-back cell built for `fiftyone_review_processed.ipynb` | 2026-08-18 | DEC-072 — student killed the stalled dedup run (`kill 48994`, confirmed dead) given a follow-up merge+dedup pass was already needed regardless (new Doors source not yet merged) and a computer restart was coming anyway. Built the missing piece flagged as "a clear gap where effort would be useless": a write-back cell that reads the live, App-edited FiftyOne dataset and saves it back to YOLO `.txt` format in a new `labels_reviewed/` staging folder (not the real `labels/` — promotion is a manual, separate step). Load cell also now stashes `source_label_filename` per sample and surfaces `box_audit.py`'s actual flag reason strings (`flag_reasons`), not just a boolean. Verified for real: simulated a delete/move/add across 3 of the 750 elevator_status_s4lrk samples, write-back reported `added=1 removed=1 modified=1 unchanged=747` exactly as expected, and confirmed byte-for-byte that the original `labels/` files were never touched |
| `docs/OPEN_QUESTIONS.md` #1 fully closed — `ronits-workspace-e52mh/nyb` dropped, `door_detection_zqt59`'s `hinged` class confirmed hardware, license confirmed for both new Roboflow sources | 2026-08-18 | DEC-073 — student's own visual/page review resolved all three remaining sub-items: (1) `ronits-workspace-e52mh/nyb` dropped, not worth pursuing now that Trash Bins isn't the bottleneck; (2) `hinged` confirmed door-hinge hardware, not additional door instances — every `hinged` box's image already has a real `door` box, so the existing `native_class_filter: "door"` was already correct, no code change or re-pull needed; (3) both `trashcan_detection_pihfn` and `door_detection_zqt59` confirmed **CC BY 4.0**, recorded as a new `license:` field on each `config/datasets.yaml` entry |
| Mistakenness top-N review tooling built for `fiftyone_review_processed.ipynb` (Stage 5.5) | 2026-08-18 | DEC-074 — student opted for a bounded top-1,000 review (~1hr) instead of full-22,846 or fully-deferred. New cross-source mode (mistakenness ranking spans every source, unlike the notebook's other single-`source_key` modes): re-runs yolov8n inference fresh for just the top-1,000 slice (report only stored counts, not box coordinates), reusing `run_mistakenness.py`'s own crosswalk/eligibility/box-conversion logic directly rather than reimplementing it. Shows `ground_truth` + `predictions` side by side plus a sortable `mistakenness` field. Write-back merges edits into each source's full label file rather than replacing it outright — `ground_truth` here is eligible-classes-only (7/16), so a naive replace would silently delete any other canonical class's boxes on the same image; verified against a real mixed-class file (Person+Trash Bins) that the non-eligible box survives untouched. Stage 5.7's post-merge mistakenness pass remains separately deferred, not addressed by this |
| `box_audit.py` extended with `--pool merged`; box review reordered to run post-cap/merge, not pre-cap | 2026-08-18 | DEC-075 — found the Tukey-fence heuristic flags across all 16 sources (84,950 boxes total), but only elevator/stairs (1.3% of that) had a reviewable list; also found `cap_per_class.py` doesn't use box-quality signal at all, so pre-cap review wastes effort proportional to each source's cap discard rate (crowdhuman: 96.2% of candidates discarded). New `--pool merged` mode audits `dataset/merged/` instead of raw `dataset/processed/<source>/`, recovering source/filename from `merge.py`'s own prefix; writes a full flagged list per source (not a 300-sample) using the same filename convention the notebook already reads, so no notebook changes needed. Verified via real dry-run against the current pool: crowdhuman's flagged count drops 68,430 → 133 (99.8%), elevator_status_s4lrk 844 → 645, stairs_i2yia 233 → 178; global class balance matched `merge_report.json` exactly. Real (non-dry-run) generation deliberately deferred until after the next `cap_per_class.py` → `merge.py` rerun folds in Doors — current merged pool is stale |
| DEC-018r's "near-exhaustive" Roboflow review scope resolved — risk-ranked full pass, no source skipped | 2026-08-18 | DEC-076 — checked the real scale of DEC-018r's 2026-08-06 "near-exhaustive" commitment for the first time: 30,072 images / 41,793 instances across the 12 currently-merged small Roboflow pools, cap only cut ~23% (not the 96% seen on crowdhuman). Student's call: every pool still gets a full pass (no skip, honoring the original commitment) via `flagged_report_path=None` in `fiftyone_review_processed.ipynb`, but pace varies by a risk ranking (`box_audit.py --pool merged`'s per-source flag rate, cross-checked against DEC-031's Stairs/Elevator finding) — no hard thorough/skim cutoff. `roboflow_escalator_stairs`'s outsized 7,560 images confirmed a structural side-effect of feeding two capped classes (Stairs + Escalator, `merge.py` unions both selections), not a cap bug. `docs/OPEN_QUESTIONS.md` #4 updated to reflect the resolved method (was previously marked "Resolved" on tooling availability alone, method was still genuinely undecided). Confirmed with the student separately: `elevator_awvus`'s DEC-031 known-issue status does NOT override its measured rank — ranking stays purely numeric, human judgment applies during the actual review pass |
| `cap_per_class.py` + `merge.py` rerun for real — Doors folded in, ratio invariant resolved | 2026-08-18 | DEC-077 — first real merge to include `door_detection_zqt59`. Doors: 1,337 → 5,830 candidates → 4,500 selected images / 5,029 instances (hit the hard cap for the first time, no longer candidate-starved). **Ratio invariant now met**: max=Vehicle(4,500)/min=Trash Bins(1,663) = 2.71 (≤3.0) — closes `docs/OPEN_QUESTIONS.md` #1 for good, neither fallback needed. Merged pool grew 52,756 → 55,917 images, verified on disk exactly matching the report; `door_detection_zqt59` contributes 3,460 of Doors' new 4,500. Unblocks `box_audit.py --pool merged` (DEC-075) for a real, non-dry-run run — the pool it was deferred on is no longer stale |
| `box_audit.py --pool merged` run for real for the first time, against the post-Doors pool | 2026-08-18 | Every one of the 18 sources now has its own full flagged-boxes list at `dataset/reports/<source>_flagged.json` (previously only elevator/stairs had one, per DEC-057/070). `door_detection_zqt59` gets its first-ever flagged list: 597/3,760 boxes (15.88%), immediately taking #1 in DEC-076's risk ranking — table updated in DEC-076 with all 13 small-pool sources now ranked. `merged_box_audit_report.json` also written. Next: the student's risk-ranked full review pass (DEC-076) can begin for real, now that every source's list reflects the actual final pool |
| Whole-image removal added: tag `exclude` in the App, `merge.py` reads it | 2026-08-18 | DEC-078 — found sample-deletion-in-the-App would've been a silent no-op against the existing write-back cell (deleted samples just aren't iterated, nothing stops the original file from surviving untouched). Verified a simpler, more robust alternative first: FiftyOne's native App tagging (confirmed via docs.voxel51.com, no plugin). Write-back cell now tracks `"exclude" in sample.tags` per sample, merges into `dataset/reports/<source>_excluded.json` (accumulates across sittings). New `merge.py:load_excluded_pairs()` subtracts these from `cap_report.json`'s selected union — `cap_per_class.py` is not re-run, an excluded image's quota slot is simply lost, not backfilled. Verified for real: smoke-tested 2 real `trashcan_detection_pihfn` filenames, union dropped 55,917 → 55,915 exactly, smoke-test file cleaned up after |
| Predictions overlay generalized to full-pool review (`show_predictions`); `accept` label-tag promotes a prediction into ground truth | 2026-08-18 | DEC-079 — student confirmed the 7/16 COCO-class ceiling is fine (worried classes are exactly the COCO-eligible incidental ones, matching `docs/preprocessing.md`'s existing but underused procedure). Checked why the mistakenness section caps at 1,000 before generalizing: benchmarked real yolov8n inference at 32.4 img/s on this machine — even `escalator_stairs` (7,560 images, the largest small pool) is under 4 minutes, confirming the cap was a student review-TIME budget (DEC-074), not an inference-cost limit, so the new full-pool `show_predictions` toggle runs uncapped per source. Acceptance mechanism verified against real FiftyOne docs first (native per-*label* tagging exists, no plugin, no built-in field-to-field copy) then built: tag a predicted box `accept`, write-back promotes it into `ground_truth` before writing — predictions never persist to disk themselves, so no redundant-box risk. Verified for real: found 4 real predictions across 10 real trashcan images, simulated accept-tag promotion (ground_truth 3→4 boxes) and exclude-tag (DEC-078) coexisting cleanly on the same sample |
| Full-pool review restricted to the merged (post-cap) pool by default (`restrict_to_merged`) | 2026-08-19 | DEC-080 — student caught a real discrepancy while actually using the notebook: `door_detection_zqt59` reviewed as 4,493 images, but only 3,460 were ever selected into `dataset/merged/`. Root cause: the full-pool browse mode (`flagged_report_path=None`) had no awareness of `cap_report.json`'s selection at all — the exact problem DEC-075 already fixed for `box_audit.py`, just never applied to this notebook's own browsing mode (the flagged-mode path was accidentally already correct, since `box_audit.py --pool merged` only scans `dataset/merged/`). New `restrict_to_merged` toggle (default True) filters the full-pool browse to only images this source actually contributed to `dataset/merged/`. Verified for real: door_detection_zqt59 — 4,493 total, 1,033 correctly skipped, 3,460 reviewed, exact match |
| `dataset.classes` set on every review dataset — fixes App's "Import your dataset schema" block on drawing new boxes | 2026-08-19 | DEC-081 — student hit a real wall on their 2nd source (`cv_project_hovyc`): existing-box edits and accept/exclude tagging worked, but drawing a brand-new box in the Annotate tab was blocked by a schema-import prompt. Student's own shared field-schema JSON pinpointed the gap directly — no `classes` list anywhere for `ground_truth`/`predictions`. Confirmed `dataset.classes` (a real, long-standing, non-Enterprise SDK property, not the newer Ontology system) was empty (`{}`) on the live dataset. Fixed immediately on both live datasets without requiring a rebuild (`dataset.classes = {...}` + `dataset.save()`, verified persisted by reloading fresh from the DB in a separate process), and added the same 2 lines permanently to the build cell so every remaining source gets it automatically |
| `stairs_i2yia`/`escalator_stairs`/`elevator_status_s4lrk` failed on direct visual review; `stair_gaptw` + `elevator_status_0iq4p` added, pulled, converted; Escalator's schema slot reserved | 2026-08-20 | DEC-082 — student's full-pool review (DEC-076) surfaced a defect worse than box-shape polish: boxes not on the annotated object at all (not just imperfect shape), duplicate/near-duplicate images, and (for elevator_status_s4lrk) corrupted black regions baked into the source images. Independently re-verified with fresh samples before accepting — 3/3 on each of the 3 failing sources, plus `elevator_awvus` confirmed clean as a comparison point — and ruled out a conversion-pipeline bug first (raw vs. converted coordinates matched exactly). All 3 marked `audit_status: failed`. Two replacements found by the student, SDK-checked, pulled, converted: `stair_gaptw` (1,564 images, single clean class) and `elevator_status_0iq4p` (1,461 images/2,868 boxes, 4 state-style native classes — "People are in the elevator." deliberately split to canonical Person rather than Elevator, geometrically verified post-convert on real samples, not assumed). Real process finding: both new sources' licenses (Public Domain, CC BY 4.0) were recoverable from the real downloaded `data.yaml`'s `roboflow.license` field even though the SDK's `Project.license` attribute returned `None` for both — worth checking that file directly for future sources instead of stopping at "SDK doesn't expose it." **Escalator now has zero active source** — checked `config_loader.py` before deciding how to represent this (hardcoded `EXPECTED_NC`/`CANONICAL_NAMES`, `Tricycle`'s `status: possible` confirmed to be documentation-only, not an exclusion mechanism); student's call: reserve the slot for now, brainstorm a replacement class, renumber to 15 only if nothing suitable is found. **Stairs' realistic pool is now just `stair_gaptw` alone (1,564), barely above the 1,500 floor.**
| Pole switched to Open Images "Street light"; Escalator's reserved slot filled with "Shelf"; 5 new Roboflow sources added (Potholes/Pedestrian Lane/Vehicle/Motorcycle/Tricycle); 4 superseded Roboflow sources benched | 2026-08-20 | DEC-083 — Shelf chosen over Cart/Countertop after visual inspection in `fiftyone_preview.ipynb`'s new ad-hoc candidate section (Cart: mostly horse-drawn carts; Countertop: mostly kitchen counters). `classes.yaml` id 6 renamed Escalator→Shelf, `config_loader.py`'s hardcoded `CANONICAL_NAMES` updated in sync, re-verified via `config_loader.py`'s own validation. Pole restructured from 2 unreviewed Roboflow sources to a single Open Images primary, same pattern as Chairs/Tables. 5 new Roboflow sources SDK-checked, pulled, converted for real — caught and fixed a real bug where a bare-list `native_class_filter` (unsupported shape) silently blanket-mapped every native class instead of filtering, caught from the conversion report's own dropped-count before trusting it. One genuinely ambiguous call (motorcycles mixed into a `vehicle` label with no sub-filter) asked directly rather than guessed. Reinstate-if-insufficient conditional resolved for all 3 affected classes — none needed it (Potholes 2,661, Pedestrian Lane 2,745, Stairs 2,294). Found and fixed a real DEC-number collision from a concurrent session (renumbered to DEC-084) |
| Post-DEC-083 verification pass — real `wtf_dwvgm` stale-file bug found and fixed, `classes.yaml` documentation resynced across 7 classes, one pre-existing floor-compliance risk flagged | 2026-08-21 | DEC-085 — student asked to re-verify DEC-082/083/084 rather than trust the summary; re-derived every number from real files instead of re-reading the prior write-up. Found `wtf_dwvgm` had 4 stale, mislabeled files on disk left over from the pre-fix buggy run (`yolo_to_intermediate.py`'s converter writes newly-qualifying files but never sweeps ones that stop qualifying after a mapping change) — deleted, now matches its own report exactly; Pedestrian Lane's active total corrected 2,745 → 2,741 as a direct consequence. Separately found `classes.yaml`'s per-class provider documentation had gone stale for 7 classes (only Pole/Shelf were kept in sync during the original work) — confirmed via `get_eligible_projects()` that this never affected pipeline behavior (Roboflow acquisition/conversion reads `datasets.yaml`, not `classes.yaml`'s provider lists) but was actively misleading as documentation; resynced all 7, plus `doors:` (a gap from an earlier session, DEC-071/073). Flagged, not resolved: Potholes' 2,661 total depends on `dataset_ninja_road_damage_detector` counting as active, but that source has no `audit_status` field and its own notes describe it as a dormant DEC-034 fallback — without it, Potholes sits at 1,330, below the 1,500 floor. Added to `docs/OPEN_QUESTIONS.md` #11 for the student's call. Everything else re-checked matched exactly |
| `dataset_ninja_road_damage_detector` activated for Potholes; real Stage-5.4 gating bug found and fixed — `cap_per_class.py` had no `audit_status` awareness at all | 2026-08-21 | DEC-086 — student's call on DEC-085's flagged question: activate the fallback. Set explicit `audit_status: approved`. Tracing the actual effect of that flag surfaced a much bigger bug: `discover_processed_sources()` → `build_class_index()` → `cap_class()` scanned every `dataset/processed/<source>/` unconditionally, no `audit_status` check anywhere in Stage 5.4 — meaning `stairs_i2yia`/`escalator_stairs`/`elevator_status_s4lrk`/`pole_detection_z76mb`/`utility_poles_44tzx`/`pothole_vhmow`/`pedestrian_and_animal_crossing` (all benched/failed after their first pull, DEC-082/083) would have silently re-entered the candidate pool on the next real cap/merge run, contradicting what those entries claimed. Fixed with new `get_inactive_processed_source_keys()` in `config_loader.py`, wired into `cap_per_class.py` only — deliberately not the shared `discover_processed_sources()`, since `box_audit.py`'s diagnostic mode legitimately still wants visibility into failed sources. Verified via `--dry-run`: Potholes (2,661), Stairs (2,294), Pedestrian Lane (2,741), Pole (2,154) candidates all land exactly on DEC-083/085's already-documented numbers, all 7 sources correctly excluded, ratio invariant still holds (2.71). Confirmed the last real run (DEC-077, 8/18) predates this risk and isn't retroactively contaminated — this only affects the still-pending next run |
| Person/Vehicle/Motorcycle priority-source order revised; `me5_u6rvg`/`augmented_tricycle` benched per review-checklist policy; `cv_project_hovyc`/`trashcan_detection_pihfn` cleaned labels promoted, revealing real multi-class enrichment | 2026-08-21 | DEC-087 — `CLASS_PRIORITY_SOURCES` updated: Person → exdark/revised_pedestrian_obstacle/crowdhuman/open_images; Vehicle → dlsu_d_vehicle_type_detection/exdark/revised_pedestrian_obstacle/open_images; Motorcycle (new) → dlsu_d_vehicle_type_detection/exdark/open_images. A `--dry-run` before finalizing caught Open Images being squeezed to 0 selected images for Person/Vehicle — flagged via AskUserQuestion, student chose an explicit guaranteed floor for it too (375-500/class) rather than pure leftover. `me5_u6rvg`/`augmented_tricycle` benched (both `[x]`/unreviewed on the review checklist, student's explicit call); `elevator_awvus` deliberately kept active despite its own `[x]` mark — DEC-082 already independently confirmed it clean, checklist's stale DEC-031-era note corrected rather than the source being blindly dropped. Promoted `cv_project_hovyc`/`trashcan_detection_pihfn`'s `labels_reviewed/` over the real `labels/` (originals backed up to `dataset/backups/` first — `dataset/processed/` is gitignored). That promotion surfaced an unexpected candidate-count shift in unrelated classes (Pole/Stairs/Trash Bins/Pedestrian Lane) — investigated fully before trusting it (ruled out a concurrent-session change via mtime checks, then found both sources are no longer single-class: the review added real, correctly-boxed content for other visible classes in the same photos). Spot-checked 2 real samples to confirm legitimacy, not review mistakes. Notebook checklist rewritten (v9→v10) to reflect all of this |
